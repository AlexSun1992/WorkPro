import { notifyListeners } from "./listeners.service.js";

it("listeners to be called", () => {});

it("listener is a function only", () => {});

it("all functions fired despite error in one of them", () => {});
